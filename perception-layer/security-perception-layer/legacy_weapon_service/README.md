# Legacy Weapon Service

This directory contains an old TensorFlow 1.x wrapper that is no longer used by the current app.

The external legacy model repository was removed from the workspace, so this service is currently inactive. The active weapon model is the YOLO setup documented in the project README.
